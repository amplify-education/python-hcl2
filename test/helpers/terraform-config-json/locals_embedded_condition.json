{
  "locals": [
    {
      "terraform": {
        "channels": "${(local.running_in_ci ? local.ci_channels : local.local_channels)}",
        "channels_object": "${(local.running_in_ci ? {channels = local.ci_channels} : {channels = local.channels})}",
        "authentication": [],
        "foo": null
      }
    }
  ]
}
